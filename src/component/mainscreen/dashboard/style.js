import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles(theme => ({
  
    root: {
      flexGrow: 1,
    },
   link: {
    color:'white', 
    textDecoration:'none',
    marginTop:'20px',
   },
  }));